# Netlify et hébergeurs compatibles : HTML toujours revalidé (évite pages figées).
# GitHub Pages seul ignore ce fichier — utiliser Cloudflare ou un rechargement forcé (voir index.html).
/**/*.html
  Cache-Control: public, max-age=0, must-revalidate
